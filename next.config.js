/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['sempitern.com'], // Add the hostname(s) where your images are hosted
  },
}

module.exports = nextConfig
